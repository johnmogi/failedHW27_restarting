


	צרו פונקציה בשם getRandomEvenNumber המייצרת מספר רנדומלי.
	על הפונקציה לקבל לסוגריים את ה-limit - מה המספר המקסימלי שיש להגריל.
	אם המספר זוגי - הצליחה - ולכן מדווחת אותו ע"י resolve (ה-successCallback)
	אם המספר אי-זוגי - לא הצליחה - ומדווחת אותו ע"י reject (ה-errorCallback)
